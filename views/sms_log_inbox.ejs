<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SMS</title>

</head>
<body>
      <% if (rows && rows.length) { sn=0; %>
	    
        <% rows.forEach(row => {
		%>
          <tr>
		   <td>
             <img src="assets/images/sms.svg" alt="" />
            </td>
			<td><%= row.rid %></td>
			 <td><%= row.mobile_no %></td>
			 <td><%= row.smsdate %></td>
             <td><%= row.smstime %></td>
			  <td><%= 'RECEIVE' %></td>
			<td>
             <img src="assets/images/view.svg" alt=""  style="cursor:pointer"  onclick="opensmsModal('<%= row.message %>')"/>
           </td>
          </tr>
        <% }); %>
      <% }  %>
	   
</body>
</html>