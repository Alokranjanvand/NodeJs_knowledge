<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SMS</title>

</head>
<body>   
   <form action="" class="form-horizontal">
    <div class="form-group">
    <div class="col-sm-10">
    <select id="sms_template" class="form-control" onchange="get_smsbody(this.value)">
     <option>-SELECT TEMPLATE-</option>
	   <% if (rows && rows.length) {%>
	    
			<% rows.forEach(row => { %>
				<option value="<%= row.id + '~' + row.route_id+ '~' + row.template_id %>"><%= row.sms_type %></option>
			 <% }); 
		}
		%>
    </select>
    </div>
    </div>
    <input type="hidden" name="route_id" id="route_id" value="" />
    <input type="hidden" name="template_id" id="template_id" value="" />
    <input type="hidden" name="config_id" id="config_id" value="" />
    <input type="hidden" name="url_to_tiny" id="url_to_tiny" value="" />
    <input type="hidden" name="rid" id="rid" value="<?php echo $rid; ?>" />
    <input type="hidden" name="clientIDsms" id="clientIDsms" value="<?php echo $code; ?>" />
    <div class="form-group">
    <div class="col-sm-10"><input type="text" name="phoneTo" id="phoneTo" placeholder="Phone number" value="<%=phone_no; %>" class="form-control text ui-widget-content ui-corner-all" /></div>
    </div>
    <div class="form-group">
    <div class="col-sm-10"><textarea id="phoneBody" name="phoneBody" class="form-control" placeholder="Message..."></textarea></div>
    </div>
	<div id="alert_sms"></div>	
	<div class="form-group">  
        <input type="button" id="Email_send" value="SMS Send" onclick="send_sms();"/>   
	</div>
    </form>
	</body>
</html>